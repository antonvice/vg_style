# cog.yaml

build:
  python_version: "3.10"
  python_packages:
    - "torch==1.12"
    - "diffusers==0.11.1"
    - "transformers==4.25.1"
    - "datasets==2.8.0"
    - "pandas==1.5.2"
  system_packages:
    - git
    - wget
    - unzip

predict: "predict.py:Predictor"